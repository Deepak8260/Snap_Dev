apiVersion: v1

kind: Service

metadata:
  name: snapdev-svc
  namespace: snapdev-ns

spec:
  type: NodePort
  selector:
    app: snapdev-label

  ports:
    - protocol: TCP
      port: 5000 # cluster port
      targetPort: 5000 # container port
      nodePort: 30080
